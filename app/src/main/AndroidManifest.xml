<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bangkit.healthtroops.ekipi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EKIPI"
        android:usesCleartextTraffic="true">
        <!-- should disable usesCleartextTraffic in release -->
        <activity android:name=".ui.dailyform.DailyFormActivity" />
        <activity android:name=".ui.camera.CameraActivity" />
        <activity android:name=".ui.profileedit.ProfileEditActivity" />
        <activity android:name=".ui.splash.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.auth.AuthActivity" />
    </application>

</manifest>
